const html = document.documentElement;

const navbar = document.getElementById("navbar");

const burger_menu = document.getElementById("burger-menu");
const menu = document.getElementById("menu");
const menu_buttons = document.querySelectorAll(".menu-button");
const exit_menu = document.getElementById("exit-menu");

const projects_link = document.getElementById("projects-link");
const projects_preview = document.getElementById("projects-preview");
const projects_list = document.querySelectorAll(".project-element");
const projects_images = document.querySelectorAll(".project-image");
